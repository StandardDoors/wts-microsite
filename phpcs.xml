<?xml version="1.0"?>
<ruleset name="WTS Landing Pages">
    <description>Coding standards for WTS Landing Pages</description>

    <!-- Scan these files -->
    <file>src</file>
    <file>tests</file>
    <file>build.php</file>

    <!-- Exclude vendor directory -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/dist/*</exclude-pattern>
    <exclude-pattern>*/partials/footer.php</exclude-pattern>

    <!-- Use PSR-12 as base standard -->
    <rule ref="PSR12"/>

    <!-- Docblock requirements (relaxed for practicality) -->
    <rule ref="Squiz.Commenting.FunctionComment">
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingReturn"/>
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop"/>
        <exclude name="Squiz.Commenting.FunctionComment.SpacingAfterParamType"/>
        <exclude name="Squiz.Commenting.FunctionComment.ThrowsNoFullStop"/>
        <exclude name="Squiz.Commenting.FunctionComment.Missing"/>
        <exclude-pattern>*/partials/*</exclude-pattern>
    </rule>

    <rule ref="Squiz.Commenting.ClassComment">
        <exclude name="Squiz.Commenting.ClassComment.TagNotAllowed"/>
        <exclude name="Squiz.Commenting.ClassComment.Missing"/>
        <exclude name="Squiz.Commenting.ClassComment.SpacingAfter"/>
    </rule>

    <!-- File-level docblock (not strictly required) -->
    <rule ref="Squiz.Commenting.FileComment">
        <exclude name="Squiz.Commenting.FileComment.Missing"/>
        <exclude name="Squiz.Commenting.FileComment.MissingPackageTag"/>
        <exclude name="Squiz.Commenting.FileComment.MissingAuthorTag"/>
        <exclude name="Squiz.Commenting.FileComment.SpacingAfterOpen"/>
    </rule>

    <!-- Line length (relaxed for HTML content in PHP files) -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
        <exclude-pattern>*/partials/*</exclude-pattern>
    </rule>

    <!-- Partials may have unconventional structure due to HTML output -->
    <rule ref="PSR1.Files.SideEffects">
        <exclude-pattern>*/partials/*</exclude-pattern>
        <exclude-pattern>build.php</exclude-pattern>
    </rule>

    <!-- Build script is standalone, doesn't need namespace -->
    <rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">
        <exclude-pattern>build.php</exclude-pattern>
    </rule>

    <!-- Show progress and colors -->
    <arg value="sp"/>
    <arg name="colors"/>

    <!-- Use short array syntax -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
</ruleset>
